# Majdoor Awaaz V2 merge

The `platform-v2` branch keeps the existing repository history while making the V2 Digital Employee Platform the GitHub Pages root entry point. The previous `main` version remains recoverable from Git history.
